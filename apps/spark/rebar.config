{erl_opts, [debug_info,
            {parse_transform, niffy_transform},
            {niffy_options, [{compiler, "gcc"},
                             {flags, ["-Werror"]},
                             {c_dir, "src_c"},
                             {strict_types, false}]}
]}.

{deps, []}.